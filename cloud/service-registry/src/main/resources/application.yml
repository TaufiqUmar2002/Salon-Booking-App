server:
  port: 4001

spring:
  application:
    name: eureka-server




eureka:
  instance:
    hostname: localhost
  client:
    register-with-eureka: false
    fetch-registry: false

#eureka:
#  instance:
#    hostname: eureka-server
#    prefer-ip-address: true
#
#  client:
#    register-with-eureka: false
#    fetch-registry: false
