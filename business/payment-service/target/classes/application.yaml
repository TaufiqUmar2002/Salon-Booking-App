server:
  port: 5003

spring:
  application:
    name: PAYMENT-SERVICE
  devtools:
    add-properties: true

  datasource:
    url: jdbc:postgresql://localhost:6001/app_db?options=-c%20TimeZone=UTC
    username: admin
    password: admin
    driver-class-name: org.postgresql.Driver
#  datasource:
#    url: jdbc:postgresql://postgres-db:5432/app_db
#    username: admin
#    password: admin
#    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect

eureka:
  instance:
    prefer-ip-address : true
  client:
    fetch-registry : true
    register-with-eureka : true
    service-url:
      defaultZone : http://localhost:4001/eureka/


logging:
  level:
    org.springframework.cache: TRACE
    org.hibernate.SQL: DEBUG
    org.hibernate.orm.models: DEBUG

#eureka:
#  client:
#    service-url:
#      defaultZone: http://eureka-server:4001/eureka/
#    register-with-eureka: true
#    fetch-registry: true
#
#  instance:
#    prefer-ip-address: true
